[phases.setup]
nixPkgs = ['deno']

[phases.install]
cmds = [
    'deno task install'
]

[phases.build]
cmds = [
    'deno task build'
]

[start]
cmd = 'deno task start'

[nixpacks]
start-phase = "start"