[phases.setup]
nixPkgs = ['bun']

[phases.install]
cmds = [
    'bun install --production',
    'bun add -d tsconfig-paths'
]

[phases.build]
cmds = [
    'bun run build'
]

[start]
cmd = 'bun start'

[meta]
expose = '3000'